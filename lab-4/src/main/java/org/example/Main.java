package org.example;

import org.example.models.Fork;
import org.example.models.Philosopher;

public class Main {
    static void main(String args[]) {
        // All simulations are set up inside the simulation folder
    }
}
